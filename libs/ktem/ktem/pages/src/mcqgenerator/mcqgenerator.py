def generate_mcqs(text, num_questions=15):
    try:
        # 原有的 MCQ 生成逻辑
        # ...
    except Exception as e:
        print(f"发生错误: {str(e)}")
        # 返回 63 个默认值
        return ["发生错误: " + str(e)] + [None] * 62

    # 如果一切正常,确保返回 63 个值
    # ...
